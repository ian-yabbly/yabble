#!/bin/bash
set -e
source $(dirname $0)/include

if test $APP_ENV == 'prod'; then
  fatal "Do not run this in prod!"
fi

dropdb $DB_NAME || echo "$DB_NAME database not dropped because it does not exist"
